# Optional proxy list download URL
PROXY_LIST_URL=

# Proxy enforcement defaults to true when unset.
# Set this to false only if you want the server to continue without proxies.
REQUIRE_PROXIES=true

# Optional server settings
HOST=127.0.0.1
PORT=5000
FLASK_DEBUG=0
