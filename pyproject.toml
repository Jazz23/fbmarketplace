[project]
name = "fbmarketplace"
version = "0.1.0"
description = "Facebook Marketplace scraping API"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "flask>=3.1,<4",
  "requests>=2.33,<3",
]

[dependency-groups]
dev = [
  "pytest>=8.3,<9",
]
